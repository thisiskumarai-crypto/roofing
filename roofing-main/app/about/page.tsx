import { AboutPage } from "../components";

export const metadata = {
  title: "About Us | roofY",
  description: "Meet the team building precision AI systems that answer every call and track every lead so roofing contractors can focus on real work.",
}

export default function Page() {
  return <AboutPage />;
}
